# Mobile

The Expo / React Native app is in:

```sh
cd mobile/stop-your-scroll
```

Main project documentation is available in the repository root:

```txt
../README.md
```

Quick start:

```sh
cd stop-your-scroll
npm install
npm start
```
